import 'package:flutter/material.dart';

class OstrumCommentsWidget extends StatelessWidget {
  const OstrumCommentsWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('OstrumComments Widget'),
      ),
    );
  }
}
